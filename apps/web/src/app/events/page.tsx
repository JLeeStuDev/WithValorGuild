export default function EventsPage() {
    return (
        <main>
            <h2 className="text-xl font-semibold">Events</h2>
            <p className="text-sm text-gray-600">Create, RSVP, and export to iCal (soon).</p>
        </main>
    );
}